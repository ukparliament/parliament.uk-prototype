- page_title = title("#{@person.display_name} - #{t('parties_title')} #{t('past_present')}")

%h1= page_title

%ol.list
  = render :partial => 'people/party_membership', collection: @party_memberships


