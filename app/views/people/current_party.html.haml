%h1= @person.full_name

- unless @party.nil?
  %h3= t('.title', current: t('current'), party: t('party'))
  %ol
    %li= link_to(@party.name, party_path(@party.graph_id))
    %li= "#{l(@party.party_memberships.first.start_date, format: :default)} - " unless @party.party_memberships.first.start_date.nil?
