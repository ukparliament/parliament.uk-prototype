%h1= @person.full_name

%h3= t('.title')
%p= link_to(@party.name, party_path(@party.graph_id))
%p
  = "#{l(@party.party_memberships.first.start_date, format: :default)} - " unless @party.party_memberships.first.start_date.nil?
