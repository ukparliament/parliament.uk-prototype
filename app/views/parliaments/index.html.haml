%section.section--primary
  .container
    %h1 Parliaments
    %ul.list--box
      - @parliaments.each do |parliament|
        %li
          %h2= link_to "#{parliament.date_range(date_format: '%Y')}", parliament_path(parliament.graph_id)
          %p= "#{t('parliament').capitalize} #{parliament.number}"
