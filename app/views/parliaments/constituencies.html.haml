.section--primary
  .container
    = render partial: 'pugin/components/navigation-letter', locals: { route_symbol: :parliament_constituencies_a_z_letter_path, route_args: [@parliament.graph_id] }

%h2= "#{@parliament.number} (#{@parliament.date_range(date_format: '%Y')})"

%ul
  - @constituencies.each do |constituency|
    %li
      %h2= link_to constituency.name, constituency_path(constituency.graph_id)
      - if constituency.members.empty?
        %p= t('constituencies.shared_translations.vacant').capitalize
      - else
        %p= "#{constituency.members.first.display_name} #{t('mp')}"