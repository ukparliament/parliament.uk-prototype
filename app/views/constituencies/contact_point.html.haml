%h2= @constituency.name

%h3= t('contact_points.index.title')

- @constituency.contact_points.each do |contact_point|
  = render partial: 'contact_points/contact_point', locals: { contact_point: contact_point }
