%ul.list
  - @constituencies.each do |constituency|
    %li.list__item
      %ul
        %li= link_to constituency.name, constituency_path(constituency.graph_id)
        %li= constituency.current? ? t('current_constituency') : t('past_constituency')
