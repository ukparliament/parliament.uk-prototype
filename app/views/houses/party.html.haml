%h1= @house.name
%h3= @party.name

%h4= link_to('Members', house_party_members_path(@house.graph_id, @party.graph_id))
%h4= link_to('Current Members', house_party_members_current_path(@house.graph_id, @party.graph_id))
