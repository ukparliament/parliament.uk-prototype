.section--primary
  .container
    %h1= title(t('.title', current_and_former: t('current_and_former').capitalize, parties_and_groups: t('parties_and_groups')))

    = render partial: 'pugin/components/navigation-letter', locals: { route_symbol: :parties_a_z_letter_path }

%section#content
  .container
    = render 'party_list'
