- page_title = title("#{@party.name} - #{t('.title', current_and_former: t('current_and_former').capitalize, member_plural: t('member_plural'))}")

%h1= page_title

= render partial: 'pugin/components/letter-navigation', locals: { route_symbol: :party_members_a_z_letter_path, primary_id: @party.graph_id }
= render partial: 'pugin/cards/person-list', locals: { people: @people, letter: params[:letter] }
