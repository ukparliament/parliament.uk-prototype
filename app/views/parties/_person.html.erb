<li>
  <%= link_to person_path(person.id) do %>
      <% person.display_name %>
  <% end %>

  <ul>
    <%= render :partial => 'party_memberships/party_membership', :collection => person.party_memberships %>
  </ul>

</li>

